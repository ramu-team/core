import SetupClient from './setup-client';

export const metadata = {
  title: 'Setup Kiosk - Ramu',
  description: 'Aktivasi mesin IoT Jamu Ramu',
};

export default function SetupPage() {
  return <SetupClient />;
}
